# Local Config Templates

The `config-repo/` folder contains the per-service YAML files that are ready to be copied into your external GitHub-backed Spring Cloud Config repository.

You can keep this folder in the main project during local development, or move the same files into a separate repo and point `config-server` to that location.

